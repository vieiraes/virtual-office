# Copie para .env e ajuste se precisar (ambas as variáveis têm valor padrão seguro).

# Porta do servidor Express + Socket.IO.
PORT=3001

# Diretório onde o board (sprints/cards) é persistido em JSON.
# Em produção no Fly.io, aponta para o volume montado (ver fly.toml).
DATA_DIR=./data
